{
  "rules": {
    "gotout": {
      "$email": {
        ".write": "$email === auth.email.replace('.','')"
      }
    },
    "targets": {
      "$email": {
        ".read": "$email === auth.email.replace('.','')"
      }
    },
    "out": {
      "$email": {
        ".read": "$email === auth.email.replace('.','')"
      }
    },
    "names": {
      ".read": "auth.email.matches(/.*@milton\\.edu$/)"
    },
    "classes": {
      ".read": "auth.email.matches(/.*@milton\\.edu$/)"
    },
    "numTags": {
      ".read": "auth.email.matches(/.*@milton\\.edu$/)"
    },
    "tags": {
      ".read": "auth.email.matches(/.*@milton\\.edu$/)"
    },
    ".read": "false",
    ".write": "false"
  }
}
